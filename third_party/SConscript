Import('env')

env.Library('json11', ['json11/json11.cpp'], CCFLAGS=env['CCFLAGS'] + ['-Wno-unqualified-std-cast-call'])
env.Library('kaitai', ['kaitai/kaitaistream.cpp'], CPPDEFINES=['KS_STR_ENCODING_NONE'])

# Include Rockchip libraries build (MPP, RGA, RKNN)
SConscript('SConscript_rockchip')

# Include Hailo libraries build (HailoRT, TAPPAS) - for ExoPilot 02 with Hailo-8L
SConscript('SConscript_hailo')
