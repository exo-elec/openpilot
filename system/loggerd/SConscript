Import('env', 'arch', 'soc', 'messaging', 'common', 'visionipc')

libs = [common, messaging, visionipc,
        'avformat', 'avcodec', 'avutil',
        'yuv', 'OpenCL', 'pthread', 'zstd']

# v4l_encoder.cc is Qualcomm-specific (V4L2_QCOM_*/VIDC controls from the
# deleted vendored headers) and is not built — EOP encodes via Rockchip MPP
# on RK3588 hardware (see encoderd.cc). Source kept for reference.
src = ['logger.cc', 'zstd_writer.cc', 'video_writer.cc', 'encoder/encoder.cc', 'encoder/jpeg_encoder.cc', 'encoder/ffmpeg_encoder.cc']

if arch == "aarch64" and soc == "rk3588":
  src += ['encoder/mpp_encoder.cc']
  libs += ['rockchip_mpp', 'rga']

if arch == "Darwin":
  # fix OpenCL
  del libs[libs.index('OpenCL')]
  env['FRAMEWORKS'] = ['OpenCL']

logger_lib = env.Library('logger', src)
libs.insert(0, logger_lib)

env.Program('loggerd', ['loggerd.cc'], LIBS=libs)
env.Program('encoderd', ['encoderd.cc'], LIBS=libs + ["jpeg"])
env.Program('bootlog.cc', LIBS=libs)

if GetOption('extras'):
  env.Program('tests/test_logger', ['tests/test_runner.cc', 'tests/test_logger.cc', 'tests/test_zstd_writer.cc'], LIBS=libs + ['curl', 'crypto'])
